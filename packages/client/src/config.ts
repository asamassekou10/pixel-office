export const SERVER_URL = 'http://localhost:3001';

/** World dimensions matching the office grid (each cell = 1 unit). */
export const WORLD_3D_WIDTH = 24;
export const WORLD_3D_DEPTH = 18;

/** Server coordinate space bounds. */
export const SERVER_WIDTH = 800;
export const SERVER_HEIGHT = 600;
